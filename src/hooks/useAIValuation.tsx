
// This file now serves as a re-export for backward compatibility
// to avoid having to update imports in all components at once

import { useAIValuation } from './ai/useAIValuation';

export { useAIValuation };
export default useAIValuation;
